# GrayTera Configuration

subdomain_enum:
  timeout: 10
  max_subdomains: 1000
  
  # Enable/disable strategies
  use_dns: true
  use_crt_sh: true
  use_dorking: true
  
  # Strategy-specific configs
  dns:
    threads: 10
    wordlist: wordlists/subdomains.txt
  
  ct:
    timeout: 20
  
  dork:
    pages: 5
    timeout: 10
  
  common_subdomains: ['www', 'mail', 'ftp', 'admin', 'blog', 'dev', 'api']

scope_filtering:
  enabled: true
  # Set scope file via --scope command line argument
  # Example: python main.py example.com --scope scope.json

vulnerability_scan:
  threads: 10
  timeout: 30
  max_depth: 3  # For crawling
  follow_redirects: true
  
  # Scanner-specific configs
  sqli:
    enabled: true
    payloads_file: payloads/sqli.txt
    detect_waf: true
  
  zap:
    enabled: true
    api_key: '40lq05t4hqilu3n67or858443q'
    proxy_url: 'http://127.0.0.1:8080'
    spider_enabled: true
    active_scan_enabled: true

  xss:
    enabled: false  # Enable when implemented
  
  csrf:
    enabled: false  # Enable when implemented

exploitation:
  auto_exploit: false  # Require manual confirmation
  max_attempts: 3
  timeout: 60
  
  # Exploit-specific configs
  # Exploitation Configuration
exploitation:
  auto_exploit: false
  max_attempts: 3
  timeout: 60
  
  # SQLMap configuration (Layer 1)
  sqlmap:
    path: "B:\\graytera\\GrayTera\\venv\\Scripts\\sqlmap.exe"  # Optional absolute path or command
    level: 5          # 1-5 (5 = maximum depth)
    risk: 3           # 1-3 (3 = maximum aggressiveness)
    threads: 10       # عدد الـ threads
    timeout: 600      # 10 دقائق
    crawl_depth: 3    # عمق البحث في الموقع
  
  # SQLi-specific settings
  sqli:
    extract_tables: true
    extract_users: false
    max_rows: 10
  sqli:
    extract_tables: true
    extract_users: false  # Be careful with this!
    max_rows: 10

retry:
  max_retries: 3
  backoff_factor: 2
  retry_on_status: [500, 502, 503, 504]

output:
  save_format: json  # json, html, xml
  include_raw_requests: false
  include_screenshots: false

logging:
  level: INFO  # DEBUG, INFO, WARNING, ERROR
  file: data/logs/graytera.log
  console: true
